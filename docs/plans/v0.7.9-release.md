# v0.7.9 Release Plan

**Status:** In progress on `feat/v0.7.9` (branched from `main` @ v0.7.8)

**Scope:** Edge-only. Rewrite javastraat ports to fit our design system / APIs
(do not wholesale-merge his tree). Waves only; no commit-by-commit merge-todo.

**javastraat refresh:** 2026-08-10 @ `5821134` (battery/toast set still keyed
off `5118d16`).

**Out of scope for this sprint:** DAPNET/POCSAG, DAB+, fork sidebar/PWA,
Reticulum/RNode, RF Environment companion, mobile polish until explicitly
requested.

---

## Authorship (hard rule for Waves J1 / J2)

Much of J1/J2 is his work. Preserve and call it out; do not silently absorb.

- **Git:** `Co-Authored-By: Albert Einstein <…>` / `javastraat` on substantial
  ports. Preserve any trailers already on his commits. Do not strip.
- **Commit / PR body:** name upstream hashes and “ported from
  javastraat/meshpoint”. Call out rewrite vs near-literal port.
- **Code comments:** short `Credit: javastraat/meshpoint <hash>` where we
  already use that pattern.
- **CHANGELOG:** name-free. **Discord:** credit conversationally
  (`Albert Einstein` / javastraat).

---

## Waves

### Wave J1 — javastraat core + small UI plucks

- [x] Stats donut dBm + farthest 0,0
- [x] Dashboard hash flash
- [x] MeshCore reconnect reliability (`b04e91c` / `c1bc3b8`; TxClient + source guard)
- [x] MeshCore live radio + expanded presets (`471d572` / `f912d6c`)
- [x] Serial live Region/preset/intervals/Bluetooth (`9bfbe56` / `9e06352` / `4a6055c` / `ec0c410`)
- [x] Battery `101` → "Powered" across cards/drawer/feed/chart (`29368c0`)
- [x] Battery chip green/amber/red severity tiers (`5118d16`)
- [x] Configuration GET failures toast (`676f7e3`)
- [x] Messages: `packets(packet_id, protocol)` index + WAL (`625f6cc`)
- [x] Messages TX banners → `#/configuration/transmit` (`a9b89eb`)
- [x] Shared HW model name map on node cards/drawer (`39910a0`)

**Skip**

- Blank serial-row drop (`a4cf23b`): already on our `main`
- Thermals card units / temp-without-fan (`b98582f` / `2defcd1`): no card here
- meshcore_usb auto-start when only `auto_detect` (`be780d5`): later if users hit it
- Pager / FSK / POCSAG / Reticulum / RNode / RF-env: see Park

### Wave J2 — firmware flash

- [x] Meshtastic + MeshCore esptool cards (design-system + `install.sh` care)
- [x] Configuration → Firmware page shape (shared page, version/flavor/board
  search, rescan)
- [x] Erase-everything toggle (**default off**, Meshtastic web flasher style;
  on = blank board / OS swap) (`85fb576`, intentional flip vs his default-on)
- [x] Installed firmware readout on Firmware cards (MeshCore `DEVICE_INFO`,
  Meshtastic `metadata.firmware_version`)

### Wave F — serial footguns

- [x] Soft-fail busy serial (do not kill lifespan)
- [x] Classify GPS vs radio / prefer by-path / warn when gpsd holds port
  (from `.141` ACM1 incident)

---

## Ship gate

- [ ] J1 items hardware-smoked where they touch USB
- [ ] J1/J2 ports have authorship called out (commit/PR + Discord)
- [ ] CHANGELOG Unreleased bullets (name-free)
- [ ] No version bump until gate
- Mobile is **not** a ship gate for v0.7.9

---

## Park

- DAPNET/POCSAG (including report lines `8ac8b2c` / `4cef644`)
- Heltec FSK **Pager** project
- **Reticulum / RNode** (`5821134`): LXMF, `rnsd`, Peers/Messages UI,
  Config → Reticulum, Firmware → RNode via `rnodeconf`, EU868 band plan,
  PlatformIO Heltec companion. Explicit future product call only; not Wave J2
- **RF Environment companion**
- DAB+, competing sidebar IA / PWA / `local_meshradar` map experiments

---

## Deferred

### Mobile / Wave M (until someone explicitly requests it)

Playwright MCP at 360/390/430 (+ landscape), overflow/console probes, findings
table. Seed hypotheses: Configuration cards, Updates timeline/actions, topbar
chips, Messages drill-in, node drawer / packet modal safe-area, Stats overflow.

### Post-v0.7.9 polish (fork @ `f1a7516`+)

- Config → Radio: edit cards before status (`e87700a`)
- Spectral scan / SX1261 onto Config → Radio (`a13ef73`)
- Topbar protocol chips as links (`0c160c0`)
- SVG path comma fix (`d9fe667`) if we still share those paths
- Fork-only Storage / sidebar glyph IA
- CPU temp without fan (`2defcd1`) if/when we ship a thermals card
